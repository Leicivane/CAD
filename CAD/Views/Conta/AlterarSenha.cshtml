@model CAD.Models.AlterarSenhaVM
@{
    ViewBag.Title = "Alteração de Senha";
    Layout = "~/Views/Shared/_LayoutConta.cshtml";
}

@using (Html.BeginForm("AlterarSenha", "Conta", FormMethod.Post))
{
    <div class="col-sm-6 col-sm-offset-3 col-xs-12">
        <div class="panel panel-default">
            <div class="panel-heading">Alteração de Senha - CADSYS</div>
            <div class="panel-body">
                @if (ViewBag.Mensagem != null)
                {
                    <div class="row">
                        <div class="col-md-12">
                            <div class="alert alert-success" role="alert">
                                @ViewBag.Mensagem
                            </div>
                        </div>
                    </div>
                }
                @Html.ValidationSummary(true)

                <div class="form-group">
                    @Html.LabelFor(m => m.Login, new { @class = "control-label" })
                    @Html.TextBoxFor(m => m.Login, new { @class = "form-control", data_mask = "000.000.000-00" })
                    @Html.ValidationMessageFor(m => m.Login)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.Senha, new { @class = "control-label" })
                    @Html.PasswordFor(m => m.Senha, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.Senha)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.NovaSenha, new { @class = "control-label" })
                    @Html.PasswordFor(m => m.NovaSenha, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.NovaSenha)
                </div>
                <div class="form-group">
                    @Html.LabelFor(m => m.ConfirmacaoSenha, new { @class = "control-label" })
                    @Html.PasswordFor(m => m.ConfirmacaoSenha, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.ConfirmacaoSenha)
                </div>
            </div>
            <div class="panel-footer">
                <div class="pull-left" style="margin-top: 0.5em;">

                </div>
                <div class="pull-left" style="margin: 0.5em;">
                </div>
                <div class="pull-right">
                    <input type="submit" class="btn btn-default btn-primary" value="Enviar" />
                    @Html.ActionLink("Cancelar", "Index", "Home", null, new {@class = "btn btn-default"})
                </div>
                <div style="clear: both"></div>
            </div>
        </div>
    </div>
}